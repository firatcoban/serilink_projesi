<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yönetim Merkezi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Outfit', sans-serif; }
        /* Cam Efekti (Glassmorphism) */
        .glass-card {
            background: rgba(30, 41, 59, 0.7);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
    </style>
</head>
<body class="bg-[#0f172a] text-white min-h-screen bg-[url('https://tailwindcss.com/_next/static/media/hero-dark.92751102.png')] bg-cover bg-fixed">

    <nav class="border-b border-slate-700/50 bg-[#0f172a]/80 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-20">
                
                <div class="w-1/3 hidden md:block">
                    </div>

                <div class="w-full md:w-1/3 text-center">
                    <h1 class="text-2xl md:text-3xl font-extrabold tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 to-cyan-400 uppercase drop-shadow-sm">
                        YÖNETİM MERKEZİ
                    </h1>
                </div>

                <div class="w-full md:w-1/3 flex justify-end items-center gap-3">
                    <a href="/settings" class="flex items-center gap-2 bg-slate-800 hover:bg-slate-700 border border-slate-600/50 px-4 py-2 rounded-full transition text-sm font-medium">
                        <i class="fa-solid fa-user-gear text-indigo-400"></i>
                        <span>Hesabım</span>
                    </a>
                    <a href="/logout" class="flex items-center gap-2 bg-red-500/10 hover:bg-red-500/20 text-red-400 border border-red-500/20 px-4 py-2 rounded-full transition text-sm font-medium">
                        <i class="fa-solid fa-right-from-bracket"></i>
                        <span>Çıkış</span>
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">

            <div class="lg:col-span-2 space-y-6">
                
                <div class="flex items-center gap-3 mb-4">
                    <div class="p-2 bg-indigo-500/20 rounded-lg text-indigo-400">
                        <i class="fa-solid fa-layer-group text-xl"></i>
                    </div>
                    <h2 class="text-xl font-bold text-white">Mevcut Paneller</h2>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <% users.forEach(user => { %>
                        <div class="glass-card rounded-2xl p-6 relative group hover:border-indigo-500/50 transition duration-300">
                            
                            <a href="/admin/delete-user/<%= user.id %>" onclick="return confirm('Bu paneli ve içindeki tüm linkleri silmek istediğine emin misin?')" 
                               class="absolute top-4 right-4 w-8 h-8 flex items-center justify-center rounded-full bg-slate-700/50 text-slate-400 hover:bg-red-500 hover:text-white transition opacity-0 group-hover:opacity-100">
                                <i class="fa-solid fa-trash text-xs"></i>
                            </a>

                            <div class="flex flex-col items-center text-center">
                                <div class="w-20 h-20 rounded-full p-1 bg-gradient-to-tr from-indigo-500 to-cyan-500 mb-4 shadow-lg shadow-indigo-500/20">
                                    <div class="w-full h-full rounded-full bg-slate-800 overflow-hidden">
                                        <% if(user.resim_url) { %>
                                            <img src="<%= user.resim_url %>" class="w-full h-full object-cover">
                                        <% } else { %>
                                            <div class="w-full h-full flex items-center justify-center">
                                                <i class="fa-solid fa-user text-2xl text-slate-600"></i>
                                            </div>
                                        <% } %>
                                    </div>
                                </div>

                                <h3 class="text-lg font-bold text-white mb-1"><%= user.ad_soyad %></h3>
                                <p class="text-xs text-slate-400 bg-slate-800/80 px-2 py-1 rounded-md border border-slate-700">
                                    @<%= user.username %>
                                </p>

                                <div class="mt-4 flex items-center gap-2 text-xs font-medium text-slate-300">
                                    <span class="w-2 h-2 rounded-full bg-emerald-400"></span>
                                    <%= user.link_sayisi || 0 %> Link Aktif
                                </div>

                                <a href="/admin/<%= user.username %>" class="mt-6 w-full py-3 bg-gradient-to-r from-orange-500 to-amber-500 hover:from-orange-600 hover:to-amber-600 text-white font-bold rounded-xl shadow-lg shadow-orange-500/20 transition transform active:scale-95 flex items-center justify-center gap-2">
                                    <i class="fa-solid fa-sliders"></i> YÖNET
                                </a>
                            </div>
                        </div>
                    <% }); %>
                    
                    <% if (users.length === 0) { %>
                        <div class="col-span-full text-center py-10 text-slate-500 border-2 border-dashed border-slate-700 rounded-2xl">
                            <i class="fa-regular fa-folder-open text-4xl mb-3 opacity-50"></i>
                            <p>Henüz hiç panel oluşturulmamış.</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <div class="lg:col-span-1 lg:sticky lg:top-28">
                <div class="glass-card rounded-2xl p-6 shadow-2xl border-t-4 border-t-emerald-500">
                    
                    <div class="flex items-center gap-3 mb-6 border-b border-slate-700/50 pb-4">
                        <div class="w-10 h-10 rounded-full bg-emerald-500/20 flex items-center justify-center text-emerald-400">
                            <i class="fa-solid fa-plus text-lg"></i>
                        </div>
                        <div>
                            <h3 class="text-lg font-bold text-white">Yeni Panel Oluştur</h3>
                            <p class="text-xs text-slate-400">Yeni bir profil sayfası yarat.</p>
                        </div>
                    </div>

                    <form action="/admin/create-user" method="POST" class="space-y-4">
                        
                        <div>
                            <label class="block text-xs font-bold text-slate-400 uppercase tracking-wider mb-1 ml-1">Kullanıcı Adı</label>
                            <div class="relative">
                                <span class="absolute left-4 top-3.5 text-slate-500"><i class="fa-solid fa-at"></i></span>
                                <input type="text" name="username" placeholder="örn: bugraguzelsoy" required
                                    class="w-full bg-[#0f172a] border border-slate-600 rounded-xl py-3 pl-10 pr-4 text-sm text-white focus:outline-none focus:border-emerald-500 transition">
                            </div>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-slate-400 uppercase tracking-wider mb-1 ml-1">Ad Soyad</label>
                            <div class="relative">
                                <span class="absolute left-4 top-3.5 text-slate-500"><i class="fa-solid fa-signature"></i></span>
                                <input type="text" name="ad_soyad" placeholder="Görünen İsim" required
                                    class="w-full bg-[#0f172a] border border-slate-600 rounded-xl py-3 pl-10 pr-4 text-sm text-white focus:outline-none focus:border-emerald-500 transition">
                            </div>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-slate-400 uppercase tracking-wider mb-1 ml-1">Şifre Belirle</label>
                            <div class="relative">
                                <span class="absolute left-4 top-3.5 text-slate-500"><i class="fa-solid fa-lock"></i></span>
                                <input type="password" name="password" placeholder="******" required
                                    class="w-full bg-[#0f172a] border border-slate-600 rounded-xl py-3 pl-10 pr-4 text-sm text-white focus:outline-none focus:border-emerald-500 transition">
                            </div>
                        </div>

                        <button type="submit" class="w-full mt-4 bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-3.5 rounded-xl shadow-lg shadow-emerald-600/20 transition transform active:scale-[0.98] flex items-center justify-center gap-2">
                            <i class="fa-solid fa-rocket"></i> OLUŞTUR
                        </button>

                    </form>
                </div>

                <div class="mt-6 p-4 rounded-xl bg-indigo-900/20 border border-indigo-500/20 flex items-start gap-3">
                    <i class="fa-solid fa-circle-info text-indigo-400 mt-0.5"></i>
                    <p class="text-xs text-indigo-200 leading-relaxed">
                        Oluşturduğun panellerin linklerini düzenlemek için soldaki kartlarda bulunan <b class="text-white">YÖNET</b> butonuna tıkla.
                    </p>
                </div>

            </div>

        </div>
    </div>

</body>
</html>