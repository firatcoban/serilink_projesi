<!DOCTYPE html>
<html lang="tr">
<head>
    <title>Yönetim | Seçim</title>
    <style>
        body { background: #0f172a; color: white; font-family: sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; }
        .card { background: rgba(255,255,255,0.1); padding: 20px; margin: 10px; border-radius: 10px; width: 300px; display: flex; justify-content: space-between; align-items: center; }
        .btn { background: #FF5400; color: white; text-decoration: none; padding: 10px 20px; border-radius: 5px; font-weight: bold; }
        img { width: 50px; height: 50px; border-radius: 50%; object-fit:cover; }
    </style>
</head>
<body>
    <h1>KİMİN PANELİNE GİRİLECEK?</h1>
    <% users.forEach(user => { %>
        <div class="card">
            <div style="display:flex; align-items:center; gap:10px;">
                <img src="<%= user.resim_url %>">
                <span><%= user.ad_soyad %></span>
            </div>
            <a href="/admin/<%= user.username %>" class="btn">YÖNET 👉</a>
        </div>
    <% }); %>
</body>
</html>