<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yönetim Merkezi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Outfit', sans-serif; }
        .glass-panel { 
            background: rgba(30, 41, 59, 0.7); 
            backdrop-filter: blur(20px); 
            border: 1px solid rgba(255, 255, 255, 0.08); 
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
        .sidebar-transition { transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
        .collapsed { width: 5rem; }
        .collapsed .logo-text, .collapsed .menu-text { display: none; }
        .collapsed .menu-item { justify-content: center; padding-left: 0; padding-right: 0; }
        
        .title-gradient {
            background: linear-gradient(to right, #818cf8, #c084fc, #f472b6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
    </style>
</head>
<body class="bg-[#0b0f19] text-slate-300 overflow-hidden selection:bg-indigo-500/30">

    <div class="flex h-screen relative">

        <aside id="sidebar" class="bg-[#0f172a]/50 backdrop-blur-xl border-r border-slate-800/50 flex flex-col w-72 sidebar-transition z-50">
            
            <div class="h-24 flex items-center px-8 border-b border-slate-800/50">
                <div class="flex items-center gap-3 font-bold text-2xl tracking-tight text-white group cursor-pointer">
                    <div class="w-10 h-10 rounded-xl bg-gradient-to-tr from-indigo-600 to-violet-600 flex items-center justify-center shadow-lg shadow-indigo-600/20 group-hover:scale-110 transition overflow-hidden">
                       <img src="/images/serilink.png" alt="Serilink Logo" class="w-7 h-7 object-contain invert mix-blend-screen">
                    </div>
                    <span class="logo-text font-extrabold tracking-tighter">Serilink<span class="text-indigo-500">.io</span></span>
                </div>
            </div>

            <nav class="flex-1 px-4 py-8 space-y-3">
                <p class="logo-text text-xs font-bold text-slate-500 uppercase tracking-widest px-4 mb-2">Menü</p>
                
                <a href="/admin" class="menu-item flex items-center gap-4 px-4 py-4 bg-indigo-600/10 text-indigo-400 rounded-2xl border border-indigo-500/10 transition hover:bg-indigo-600/20 group">
                    <i class="fa-solid fa-grid-2 min-w-[24px] text-center text-xl"></i>
                    <span class="menu-text font-semibold">Paneller</span>
                </a>

                <a href="/stats" class="menu-item flex items-center gap-4 px-4 py-4 text-slate-400 hover:text-white hover:bg-slate-800/50 rounded-2xl transition group">
                    <i class="fa-solid fa-chart-pie min-w-[24px] text-center text-xl group-hover:text-emerald-400 transition"></i>
                    <span class="menu-text font-medium">İstatistikler</span>
                </a>

                <a href="/settings" class="menu-item flex items-center gap-4 px-4 py-4 text-slate-400 hover:text-white hover:bg-slate-800/50 rounded-2xl transition group">
                    <i class="fa-solid fa-user-gear min-w-[24px] text-center text-xl group-hover:text-amber-400 transition"></i>
                    <span class="menu-text font-medium">Hesap Ayarları</span>
                </a>
            </nav>

            <div class="p-6 border-t border-slate-800/50">
                <a href="/logout" class="menu-item flex items-center gap-4 px-4 py-4 text-red-400 hover:bg-red-500/10 rounded-2xl transition group border border-transparent hover:border-red-500/10">
                    <i class="fa-solid fa-arrow-right-from-bracket min-w-[24px] text-center text-lg"></i>
                    <span class="menu-text font-bold">Çıkış Yap</span>
                </a>
            </div>

            <button onclick="toggleSidebar()" class="absolute -right-3 top-28 w-7 h-7 bg-slate-800 rounded-full text-slate-400 flex items-center justify-center text-xs shadow-xl border border-slate-700 hover:text-white hover:bg-indigo-600 transition z-50">
                <i id="toggleIcon" class="fa-solid fa-chevron-left"></i>
            </button>
        </aside>

        <main class="flex-1 flex flex-col h-screen overflow-hidden relative bg-[url('https://tailwindcss.com/_next/static/media/hero-dark.92751102.png')] bg-cover bg-fixed">
            <div class="absolute inset-0 bg-[#0f172a]/90 backdrop-blur-sm"></div> 
            
            <header class="h-24 px-8 flex items-center justify-between shrink-0 relative z-20 border-b border-white/5">
                
                <div class="w-1/3 hidden md:block">
                    <span class="px-3 py-1 rounded-full bg-indigo-500/10 border border-indigo-500/20 text-indigo-300 text-xs font-bold uppercase tracking-wider">
                        v3.0.1 Sistem Aktif
                    </span>
                </div>

                <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 text-center w-full md:w-auto">
                    <h1 class="text-3xl font-black title-gradient drop-shadow-lg">YÖNETİM MERKEZİ</h1>
                </div>

                <div class="w-1/3 flex justify-end items-center gap-4">
                    <div class="text-right hidden sm:block">
                        <div class="text-white font-bold text-sm"><%= typeof adminName !== 'undefined' ? adminName : 'Kullanıcı' %></div>
                        <div class="text-xs text-slate-400">Admin</div>
                    </div>
                    <div class="w-11 h-11 rounded-2xl bg-gradient-to-br from-indigo-500 to-purple-600 p-0.5 shadow-lg shadow-indigo-500/20">
                        <div class="w-full h-full bg-[#0f172a] rounded-[14px] flex items-center justify-center text-white font-bold text-lg">
                            <%= typeof adminName !== 'undefined' ? adminName.charAt(0).toUpperCase() : 'A' %>
                        </div>
                    </div>
                </div>
            </header>

            <div class="flex-1 overflow-y-auto p-8 relative z-10">
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
                    <div class="glass-panel p-6 rounded-3xl relative overflow-hidden group">
                        <div class="absolute -right-6 -top-6 w-24 h-24 bg-indigo-500/20 rounded-full blur-2xl group-hover:bg-indigo-500/30 transition"></div>
                        <p class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Toplam Panel</p>
                        <div class="flex items-end justify-between">
                            <h3 class="text-4xl font-black text-white"><%= users ? users.length : 0 %></h3>
                            <i class="fa-solid fa-layer-group text-3xl text-indigo-500/50 group-hover:scale-110 transition"></i>
                        </div>
                    </div>
                    
                    <div class="glass-panel p-6 rounded-3xl relative overflow-hidden group">
                        <div class="absolute -right-6 -top-6 w-24 h-24 bg-blue-500/20 rounded-full blur-2xl group-hover:bg-blue-500/30 transition"></div>
                        <p class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Aktif Linkler</p>
                        <div class="flex items-end justify-between">
                            <% let totalLinks = 0; if(users) { users.forEach(u => totalLinks += (u.link_sayisi || 0)); } %>
                            <h3 class="text-4xl font-black text-white"><%= totalLinks %></h3>
                            <i class="fa-solid fa-link text-3xl text-blue-500/50 group-hover:scale-110 transition"></i>
                        </div>
                    </div>

                    <div class="glass-panel p-6 rounded-3xl relative overflow-hidden group cursor-pointer hover:border-emerald-500/30 transition">
                        <div class="absolute -right-6 -top-6 w-24 h-24 bg-emerald-500/20 rounded-full blur-2xl group-hover:bg-emerald-500/30 transition"></div>
                        <p class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Sunucu Durumu</p>
                        <div class="flex items-end justify-between">
                            <h3 class="text-xl font-bold text-emerald-400 flex items-center gap-2">
                                <span class="w-3 h-3 bg-emerald-500 rounded-full animate-pulse"></span> Çevrimiçi
                            </h3>
                            <i class="fa-solid fa-server text-3xl text-emerald-500/50"></i>
                        </div>
                    </div>
                </div>

                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-8">
                    <div>
                        <h3 class="text-2xl font-bold text-white flex items-center gap-2">
                            <i class="fa-solid fa-folder-open text-indigo-400"></i> Mevcut Paneller
                        </h3>
                        <p class="text-sm text-slate-400 mt-1 pl-8">Oluşturduğun kullanıcı profillerini buradan yönet.</p>
                    </div>
                    
                    <button onclick="openModal()" class="group relative px-6 py-3 rounded-2xl bg-indigo-600 text-white font-bold shadow-lg shadow-indigo-600/25 hover:bg-indigo-500 hover:shadow-indigo-500/40 transition-all active:scale-95 overflow-hidden">
                        <div class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
                        <span class="relative flex items-center gap-2">
                            <i class="fa-solid fa-plus text-lg"></i> Yeni Panel Oluştur
                        </span>
                    </button>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 pb-20">
                    <% if(users && users.length > 0) { %>
                        <% users.forEach(user => { %>
                            <div class="glass-panel rounded-3xl p-1 relative group hover:-translate-y-1 transition duration-300">
                                <div class="bg-[#0f172a]/80 rounded-[22px] p-6 h-full flex flex-col items-center text-center relative overflow-hidden">
                                    
                                    <div class="absolute top-0 left-0 w-full h-24 bg-gradient-to-b from-indigo-500/10 to-transparent"></div>
                                    
                                    <a href="/admin/delete-user/<%= user.id %>" onclick="return confirm('Bu paneli silmek istediğine emin misin?')" 
                                       class="absolute top-3 right-3 w-8 h-8 flex items-center justify-center rounded-full bg-slate-800 text-slate-400 hover:bg-red-500 hover:text-white transition opacity-0 group-hover:opacity-100 z-10 border border-slate-700">
                                        <i class="fa-solid fa-trash text-xs"></i>
                                    </a>

                                    <div class="w-24 h-24 rounded-full p-1 bg-gradient-to-tr from-indigo-500 via-purple-500 to-pink-500 mb-4 shadow-xl shadow-indigo-500/20 relative z-10">
                                        <div class="w-full h-full rounded-full bg-slate-900 overflow-hidden border-2 border-[#0f172a]">
                                            <% if(user.resim_url) { %>
                                                <img src="<%= user.resim_url %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                                            <% } else { %>
                                                <div class="flex items-center justify-center h-full text-slate-600"><i class="fa-solid fa-user text-3xl"></i></div>
                                            <% } %>
                                        </div>
                                    </div>

                                    <h4 class="text-white font-bold text-lg leading-tight mb-1"><%= user.ad_soyad %></h4>
                                    <p class="text-xs font-medium text-indigo-400 mb-5 bg-indigo-500/10 px-3 py-1 rounded-full border border-indigo-500/20">@<%= user.username %></p>

                                    <div class="grid grid-cols-2 gap-2 w-full mb-6">
                                        <div class="bg-slate-800/50 rounded-xl p-2 border border-slate-700/50">
                                            <div class="text-[10px] text-slate-400 uppercase font-bold">Linkler</div>
                                            <div class="text-white font-bold"><%= user.link_sayisi || 0 %></div>
                                        </div>
                                        <div class="bg-slate-800/50 rounded-xl p-2 border border-slate-700/50">
                                            <div class="text-[10px] text-slate-400 uppercase font-bold">Tıklanma</div>
                                            <div class="text-white font-bold">-</div>
                                        </div>
                                    </div>

                                    <a href="/admin/<%= user.username %>" class="mt-auto w-full py-3 rounded-xl bg-slate-800 hover:bg-slate-700 text-white text-sm font-bold transition border border-slate-700 group-hover:border-indigo-500/50 group-hover:bg-gradient-to-r group-hover:from-indigo-600 group-hover:to-violet-600 group-hover:shadow-lg group-hover:shadow-indigo-500/25">
                                        Yönet & Düzenle
                                    </a>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="col-span-full py-20 text-center border-2 border-dashed border-slate-800 rounded-3xl bg-slate-900/50">
                            <div class="w-20 h-20 bg-slate-800 rounded-full flex items-center justify-center mx-auto mb-4 text-slate-600">
                                <i class="fa-regular fa-folder-open text-4xl"></i>
                            </div>
                            <p class="text-slate-400 font-medium">Henüz oluşturulmuş bir panel yok.</p>
                            <button onclick="openModal()" class="text-indigo-400 hover:text-indigo-300 text-sm font-bold mt-2 underline decoration-dashed">
                                İlk panelini şimdi oluştur
                            </button>
                        </div>
                    <% } %>
                </div>

            </div>
        </main>
    </div>

    <div id="createModal" class="hidden fixed inset-0 z-[100] bg-[#000000]/80 backdrop-blur-md flex items-center justify-center p-4">
        <div class="bg-[#1e293b] w-full max-w-md rounded-3xl border border-slate-700 shadow-2xl relative animate-[fadeIn_0.3s_ease-out]">
            
            <div class="p-8 pb-0">
                <div class="flex items-center justify-between mb-6">
                    <div>
                        <h3 class="text-2xl font-bold text-white">Yeni Panel</h3>
                        <p class="text-sm text-slate-400">Hızlıca yeni bir profil oluştur.</p>
                    </div>
                    <button onclick="closeModal()" class="w-8 h-8 rounded-full bg-slate-800 text-slate-400 hover:bg-red-500 hover:text-white transition flex items-center justify-center">
                        <i class="fa-solid fa-xmark"></i>
                    </button>
                </div>
            </div>

            <form action="/admin/create-user" method="POST" class="p-8 pt-2 space-y-5">
                <div>
                    <label class="block text-xs font-bold text-slate-400 uppercase mb-2 ml-1">Kullanıcı Adı</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-slate-500"><i class="fa-solid fa-at"></i></span>
                        <input type="text" name="username" placeholder="örn: firo" required 
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-10 pr-4 text-white focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition placeholder-slate-600 font-medium">
                    </div>
                </div>

                <div>
                    <label class="block text-xs font-bold text-slate-400 uppercase mb-2 ml-1">Görünen İsim</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-slate-500"><i class="fa-solid fa-user"></i></span>
                        <input type="text" name="ad_soyad" placeholder="örn: Fırat Çoban" required 
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-10 pr-4 text-white focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition placeholder-slate-600 font-medium">
                    </div>
                </div>

                <div>
                    <label class="block text-xs font-bold text-slate-400 uppercase mb-2 ml-1">Şifre</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-slate-500"><i class="fa-solid fa-lock"></i></span>
                        <input type="password" name="password" placeholder="••••••" required 
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-10 pr-4 text-white focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition placeholder-slate-600 font-medium">
                    </div>
                </div>

                <button type="submit" class="w-full mt-4 bg-indigo-600 hover:bg-indigo-500 text-white font-bold py-4 rounded-xl shadow-lg shadow-indigo-600/20 transition transform active:scale-[0.98]">
                    Paneli Oluştur
                </button>
            </form>
        </div>
    </div>

    <script>
        function toggleSidebar() { 
            const s = document.getElementById('sidebar'); 
            const i = document.getElementById('toggleIcon'); 
            s.classList.toggle('collapsed'); 
            if(s.classList.contains('collapsed')) { 
                i.classList.remove('fa-chevron-left'); 
                i.classList.add('fa-chevron-right'); 
            } else { 
                i.classList.remove('fa-chevron-right'); 
                i.classList.add('fa-chevron-left'); 
            } 
        }
        function openModal() { document.getElementById('createModal').classList.remove('hidden'); }
        function closeModal() { document.getElementById('createModal').classList.add('hidden'); }
    </script>
</body>
</html>