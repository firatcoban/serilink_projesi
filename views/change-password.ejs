<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Şifre Değiştir</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-[#0f172a] text-white min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-md bg-[#1e293b] rounded-3xl shadow-2xl border border-slate-700/50">
        
        <div class="px-6 py-5 border-b border-slate-700/50 flex items-center gap-3 bg-[#334155]/20 backdrop-blur-md rounded-t-3xl">
            <a href="/settings" class="text-slate-400 hover:text-white transition bg-slate-700/50 w-8 h-8 rounded-full flex items-center justify-center">
                <i class="fa-solid fa-arrow-left text-sm"></i>
            </a>
            <h2 class="text-lg font-bold text-white flex items-center gap-2">
                <i class="fa-solid fa-shield-halved text-emerald-400"></i> Şifre Değiştir
            </h2>
        </div>

        <div class="p-8">
            
            <div class="mb-6 text-center">
                <p class="text-sm text-slate-400">Hesap güvenliğin için güçlü bir şifre belirle.</p>
            </div>

            <% if (error) { %>
                <div class="bg-red-500/10 border border-red-500/50 text-red-400 p-4 rounded-xl text-sm mb-6 flex items-start gap-3">
                    <i class="fa-solid fa-circle-exclamation mt-0.5 text-lg"></i>
                    <span><%= error %></span>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="bg-emerald-500/10 border border-emerald-500/50 text-emerald-400 p-4 rounded-xl text-sm mb-6 flex items-start gap-3">
                    <i class="fa-solid fa-circle-check mt-0.5 text-lg"></i>
                    <span><%= success %></span>
                </div>
            <% } %>

            <form action="/change-password-action" method="POST" class="space-y-5">
                
                <div>
                    <label class="block text-xs font-bold text-slate-500 uppercase tracking-wider mb-2 ml-1">Mevcut Şifre</label>
                    <div class="relative group">
                        <input type="password" name="old_password" id="old_pass" required placeholder="Şu anki şifreni gir"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-4 pr-12 text-sm text-white focus:outline-none focus:border-indigo-500 transition shadow-inner">
                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center cursor-pointer" onclick="togglePassword('old_pass', this)">
                            <i class="fa-solid fa-eye text-slate-500 hover:text-white transition p-2"></i>
                        </div>
                    </div>
                </div>

                <div class="bg-slate-800/50 p-4 rounded-xl border border-slate-700/50 space-y-4">
                    <div>
                        <label class="block text-xs font-bold text-indigo-400 uppercase tracking-wider mb-2 ml-1">Yeni Şifre</label>
                        <div class="relative">
                            <input type="password" name="new_password" id="new_pass" required placeholder="Yeni şifre"
                                class="w-full bg-[#0f172a] border border-slate-600 rounded-xl py-3.5 pl-4 pr-12 text-sm text-white focus:outline-none focus:border-indigo-500 transition">
                            <div class="absolute inset-y-0 right-0 pr-3 flex items-center cursor-pointer" onclick="togglePassword('new_pass', this)">
                                <i class="fa-solid fa-eye text-slate-500 hover:text-white transition p-2"></i>
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-bold text-indigo-400/70 uppercase tracking-wider mb-2 ml-1">Yeni Şifre (Tekrar)</label>
                        <div class="relative">
                            <input type="password" name="confirm_password" id="confirm_pass" required placeholder="Yeni şifreyi doğrula"
                                class="w-full bg-[#0f172a] border border-slate-600 rounded-xl py-3.5 pl-4 pr-12 text-sm text-white focus:outline-none focus:border-indigo-500 transition">
                            <div class="absolute inset-y-0 right-0 pr-3 flex items-center cursor-pointer" onclick="togglePassword('confirm_pass', this)">
                                <i class="fa-solid fa-eye text-slate-500 hover:text-white transition p-2"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <button type="submit" class="w-full bg-gradient-to-r from-emerald-500 to-teal-600 hover:from-emerald-600 hover:to-teal-700 text-white font-bold py-4 rounded-xl shadow-lg shadow-emerald-500/20 transition transform active:scale-[0.98] mt-2">
                    <i class="fa-solid fa-check-circle mr-2"></i> ŞİFREYİ GÜNCELLE
                </button>

            </form>
        </div>
    </div>

    <script>
        function togglePassword(inputId, iconElement) {
            const input = document.getElementById(inputId);
            const icon = iconElement.querySelector('i');
            
            if (input.type === "password") {
                input.type = "text";
                icon.classList.remove("fa-eye");
                icon.classList.add("fa-eye-slash");
                icon.classList.add("text-indigo-400");
            } else {
                input.type = "password";
                icon.classList.remove("fa-eye-slash");
                icon.classList.add("fa-eye");
                icon.classList.remove("text-indigo-400");
            }
        }
    </script>
</body>
</html>