<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Şifre Değiştir 🔒</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body { background: #0f172a; color: white; font-family: 'Poppins', sans-serif; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        .box { background: rgba(30,41,59,0.9); padding: 40px; border-radius: 20px; width: 100%; max-width: 400px; border: 1px solid rgba(255,255,255,0.1); }
        input { width: 100%; padding: 12px; margin-bottom: 20px; background: rgba(0,0,0,0.3); border: 1px solid #444; border-radius: 10px; color: white; box-sizing:border-box;}
        button { width: 100%; padding: 12px; background: #FF5400; color: white; border: none; border-radius: 10px; cursor: pointer; font-weight: bold; font-size: 16px; transition: 0.3s;}
        button:hover { background: #e04a00; }
        
        a.back-link { color: #aaa; text-decoration: none; display: block; margin-bottom: 20px; font-size: 14px;}
        label { display: block; margin-bottom: 5px; color: #FFD700; font-size: 14px; }
        
        .alert { padding: 10px; border-radius: 5px; margin-bottom: 15px; text-align: center; }
        .error { background: rgba(255,0,0,0.2); color: #ff9999; border: 1px solid #ff4444; }
        .success { background: rgba(0,255,0,0.2); color: #99ff99; border: 1px solid #44ff44; }
    </style>
</head>
<body>
    <div class="box">
        <a href="/settings" class="back-link">← Ayarlara Dön</a>
        <h2 style="text-align:center; margin-bottom: 30px;">Şifre Değiştir 🔒</h2>

        <% if (error) { %>
            <div class="alert error"><%= error %></div>
        <% } %>

        <% if (success) { %>
            <div class="alert success"><%= success %></div>
        <% } %>
        
        <form action="/change-password-action" method="POST">
            
            <label>Mevcut Şifreniz</label>
            <input type="password" name="old_password" required placeholder="Eski şifreni gir">

            <div style="border-top:1px solid #444; margin: 20px 0;"></div>

            <label>Yeni Şifre</label>
            <input type="password" name="new_password" required placeholder="Yeni şifreni gir">
            
            <label>Yeni Şifre (Tekrar)</label>
            <input type="password" name="confirm_password" required placeholder="Yeni şifreni tekrar gir">
            
            <button type="submit">ŞİFREYİ GÜNCELLE ✅</button>
        </form>
    </div>
</body>
</html>