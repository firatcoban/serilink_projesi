<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= profile.ad_soyad %> | Linkler 🔥</title> <link href="https://fonts.googleapis.com/css2?family=Bangers&display=swap" rel="stylesheet">

    <style>
        :root {
            --db-orange: #FF5400;
            --db-blue: #023E8A;
            --db-gold: #FFD700;
            --bg-start: #f8931f;
            --bg-end: #fff4a3;
        }

        body {
            background: linear-gradient(0deg, var(--bg-start) 0%, var(--bg-end) 100%);
            min-height: 100vh;
            font-family: 'Helvetica Neue', Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 50px;
            margin: 0;
            overflow-x: hidden;
            /* Bulut Mouse İmleci */
            cursor: url('/images/dragonball_cloudy.png') 16 16, auto;
            position: relative;
        }

        /* --- Arka Plan Shenron --- */
        body::before {
            content: "";
            position: fixed;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 80vw; height: 80vh;
            background-image: url('/images/shenron.png');
            background-repeat: no-repeat;
            background-position: center 30%;
            background-size: contain;
            opacity: 0.15; 
            z-index: 0;
            pointer-events: none;
        }

        /* --- 7 Ejder Topu Parçacıkları --- */
        .particles {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            overflow: hidden;
            z-index: 1; 
            pointer-events: none; 
        }

        .particles span {
            position: absolute; display: block;
            background-size: contain; background-repeat: no-repeat;
            bottom: -80px; animation: rise 15s linear infinite; opacity: 0.9; 
        }

        .particles span:nth-child(1){ background-image: url('/images/dragonball_1.png'); left: 10%; width: 40px; height: 40px; animation-duration: 12s; }
        .particles span:nth-child(2){ background-image: url('/images/dragonball_2.png'); left: 20%; width: 25px; height: 25px; animation-duration: 17s; }
        .particles span:nth-child(3){ background-image: url('/images/dragonball_3.png'); left: 35%; width: 50px; height: 50px; animation-delay: 4s; }
        .particles span:nth-child(4){ background-image: url('/images/dragonball_4.png'); left: 50%; width: 30px; height: 30px; animation-duration: 20s; }
        .particles span:nth-child(5){ background-image: url('/images/dragonball_5.png'); left: 65%; width: 45px; height: 45px; animation-delay: 2s; }
        .particles span:nth-child(6){ background-image: url('/images/dragonball_6.png'); left: 75%; width: 35px; height: 35px; animation-delay: 3s; }
        .particles span:nth-child(7){ background-image: url('/images/dragonball_7.png'); left: 85%; width: 55px; height: 55px; animation-delay: 1s; }

        @keyframes rise {
            0% { transform: translateY(0) rotate(0deg); opacity: 0; }
            15% { opacity: 0.9; }
            100% { transform: translateY(-1000px) rotate(360deg); opacity: 0; }
        }

        /* --- Profil Alanı --- */
        .profile-container { text-align: center; margin-bottom: 30px; position: relative; z-index: 2; }
        
        .profile-pic {
            width: 110px; height: 110px; object-fit: cover; border-radius: 50%; 
            border: 4px solid var(--db-gold); background-color: #fff;
            margin: 0 auto 10px; display: block;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.4);
            animation: superSaiyanGlow 2.5s infinite ease-in-out;
        }

        @keyframes superSaiyanGlow {
            0%, 100% { box-shadow: 0 0 15px rgba(255, 215, 0, 0.4); border-color: var(--db-gold); }
            50% { box-shadow: 0 0 40px rgba(255, 215, 0, 0.7), 0 0 25px rgba(255, 84, 0, 0.5); border-color: #ffe57f; }
        }
        
        .username {
            font-family: 'Bangers', cursive; 
            font-size: 32px; color: #000; 
            text-shadow: 2px 2px 0px rgba(255, 84, 0, 0.3); 
            margin: 0; 
        }
        
        .bio { font-size: 15px; color: #333; margin-top: 5px; font-weight: 600; }

        /* --- Butonlar (Tok ve Şeffaf Düzen) --- */
        .links-container { width: 90%; max-width: 500px; display: flex; flex-direction: column; gap: 16px; position: relative; z-index: 2; }

        .link-btn {
            background-color: rgba(255, 255, 255, 0.85);
            color: black; text-decoration: none;
            border: 3.5px solid var(--db-blue); border-radius: 50px; padding: 16px 20px;
            display: flex; align-items: center; justify-content: space-between;
            font-weight: 800; font-size: 17px;
            box-shadow: 6px 6px 0px var(--db-orange);
            transition: all 0.2s ease-in-out;
            position: relative; overflow: hidden;
            cursor: url('/images/dragonball_cloudy.png') 16 16, pointer;
        }
        
        .link-btn:hover {
            transform: translate(-2px, -2px); 
            box-shadow: 8px 8px 0px var(--db-orange);
            background-color: rgba(255, 255, 255, 0.95); 
        }
        
        .goku-bg {
            position: absolute; top: 42%; right: -5px;
            transform: translateY(-50%) scaleX(-1); 
            height: 150%; opacity: 0; z-index: 1; 
            pointer-events: none; transition: 0.1s;
        }
        .link-btn:hover .goku-bg { opacity: 1; }

        .btn-icon, .btn-text { position: relative; z-index: 2; }
        .btn-icon { width: 32px; height: 32px; object-fit: cover; border-radius: 50%; border: 2px solid var(--db-blue); background-color: #fff; }
        .btn-text { flex-grow: 1; text-align: center; }
        
        .footer { margin-top: 20px; font-size: 12px; font-weight: bold; opacity: 0.7; color: var(--db-blue); position: relative; z-index: 2;}
    </style>
</head>
<body>

    <%- include('loader') %>

    <div class="particles">
        <span></span><span></span><span></span><span></span>
        <span></span><span></span><span></span>
    </div>

    <div class="profile-container">
        <img src="<%= profile.resim_url %>" alt="Profil" class="profile-pic">
        
        <h1 class="username"><%= profile.ad_soyad %></h1>
        
        <p class="bio"><%= profile.biyografi %></p>
    </div>
    <div class="links-container">
        <% if (links && links.length > 0) { %>
            <% links.forEach(function(link) { %>
                <a href="/git/<%= link.id %>" target="_blank" class="link-btn">
                    <img data-src="/images/goku.gif" src="" class="goku-bg" alt="Goku">
                    
                    <% if (link.platform) { %>
                        <img src="/images/<%= link.platform.toLowerCase() %>.png" class="btn-icon" onerror="this.style.display='none'">
                    <% } %>
                    
                    <span class="btn-text"><%= link.title %></span>
                </a>
            <% }); %>
        <% } else { %>
            <div style="text-align: center; font-weight: bold; background: rgba(255,255,255,0.8); padding: 20px; border-radius: 20px; border: 2px solid var(--db-blue);">
                Henüz link eklenmemiş. <br>
                <a href="/admin" style="color: var(--db-orange);">Admin Panelinden Ekle</a>
            </div>
        <% } %>
    </div>
    
    <div class="footer">Kamehameha! 💥</div>

    <script>
        const buttons = document.querySelectorAll('.link-btn');

        buttons.forEach(btn => {
            const gif = btn.querySelector('.goku-bg');
            if (!gif) return;

            const gifBaseSource = gif.getAttribute('data-src'); 

            btn.addEventListener('mouseenter', () => {
                if (gifBaseSource) {
                    const timestamp = new Date().getTime(); 
                    gif.src = gifBaseSource + "?t=" + timestamp;
                }
            });

            btn.addEventListener('mouseleave', () => {
                if (gif) gif.src = ""; 
            });
        });
    </script>
</body>
</html>