<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hesap Ayarları</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-[#0f172a] text-white min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-md bg-[#1e293b] rounded-2xl shadow-xl border border-slate-700/50">
        
        <div class="px-6 py-5 border-b border-slate-700/50 flex items-center justify-between">
            <h2 class="text-lg font-bold text-white">Hesap Ayarları</h2>
            <a href="/admin" class="text-sm text-slate-400 hover:text-white transition flex items-center gap-1">
                <i class="fa-solid fa-arrow-left"></i> Geri
            </a>
        </div>

        <div class="p-6">
            <form action="/settings/update" method="POST" enctype="multipart/form-data" class="space-y-6">
                
                <div class="flex flex-col items-center justify-center">
                    <div class="relative group">
                        <div class="w-24 h-24 rounded-full bg-slate-700 border-4 border-slate-600 overflow-hidden shadow-lg">
                            <% if(user.resim_url) { %>
                                <img src="<%= user.resim_url %>" class="w-full h-full object-cover">
                            <% } else { %>
                                <i class="fa-solid fa-user text-3xl text-slate-400 flex items-center justify-center h-full w-full"></i>
                            <% } %>
                        </div>
                        <label class="absolute bottom-0 right-0 bg-indigo-600 hover:bg-indigo-500 text-white p-2 rounded-full cursor-pointer shadow-lg transition-transform hover:scale-110 border-2 border-[#1e293b]">
                            <i class="fa-solid fa-camera text-sm"></i>
                            <input type="file" name="profil_resmi" class="hidden">
                        </label>
                    </div>
                    <p class="mt-2 text-xs text-slate-400">Profil resmini değiştirmek için ikona tıkla</p>
                </div>

                <div class="space-y-4">
                    <div class="relative">
                        <i class="fa-solid fa-user absolute left-4 top-3.5 text-slate-500"></i>
                        <input type="text" name="ad_soyad" value="<%= user.ad_soyad %>" required placeholder="Görünen İsim"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3 pl-10 pr-4 text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500 transition">
                    </div>

                    <div class="relative">
                        <i class="fa-solid fa-at absolute left-4 top-3.5 text-slate-500"></i>
                        <input type="text" name="username" value="<%= user.username %>" required placeholder="Kullanıcı Adı"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3 pl-10 pr-4 text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500 transition">
                    </div>

                    <div class="relative">
                        <i class="fa-solid fa-envelope absolute left-4 top-3.5 text-slate-500"></i>
                        <input type="email" name="email" value="<%= user.email || '' %>" required placeholder="E-Posta (Kurtarma İçin)"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3 pl-10 pr-4 text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500 transition">
                    </div>
                </div>

                <a href="/change-password" class="block w-full bg-slate-800 hover:bg-slate-700 text-center py-3 rounded-xl border border-slate-600 transition text-sm font-medium text-slate-300">
                    <i class="fa-solid fa-lock mr-2"></i> Şifre Değiştir
                </a>

                <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3.5 rounded-xl shadow-lg shadow-indigo-500/20 transition active:scale-95">
                    DEĞİŞİKLİKLERİ KAYDET
                </button>

            </form>
        </div>
    </div>

</body>
</html>