<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hesap Ayarları ⚙️</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-[#0f172a] text-white min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-md bg-[#1e293b] rounded-2xl shadow-2xl overflow-hidden border border-slate-700/50">
        
        <div class="px-8 py-6 bg-[#334155]/30 border-b border-slate-700/50 flex items-center justify-between">
            <h2 class="text-xl font-bold text-white flex items-center gap-2">
                <i class="fa-solid fa-gear text-indigo-400"></i> Hesap Ayarları
            </h2>
            <a href="/admin" class="text-sm text-slate-400 hover:text-white transition flex items-center gap-1">
                <i class="fa-solid fa-arrow-left"></i> Geri
            </a>
        </div>

        <div class="p-8">
            <form action="/settings/update" method="POST" enctype="multipart/form-data" class="space-y-5">
                
                <div>
                    <label class="block text-xs font-semibold text-indigo-300 uppercase tracking-wider mb-2">Profil Resmi</label>
                    <div class="flex items-center gap-4">
                        <div class="w-16 h-16 rounded-full bg-slate-700 flex items-center justify-center overflow-hidden border-2 border-slate-600">
                            <% if(user.resim_url) { %>
                                <img src="<%= user.resim_url %>" class="w-full h-full object-cover">
                            <% } else { %>
                                <i class="fa-solid fa-user text-2xl text-slate-400"></i>
                            <% } %>
                        </div>
                        <input type="file" name="profil_resmi" class="block w-full text-sm text-slate-400
                            file:mr-4 file:py-2 file:px-4
                            file:rounded-full file:border-0
                            file:text-sm file:font-semibold
                            file:bg-indigo-600 file:text-white
                            hover:file:bg-indigo-700
                            transition cursor-pointer
                        ">
                    </div>
                </div>

                <div>
                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Görünen İsim</label>
                    <input type="text" name="ad_soyad" value="<%= user.ad_soyad %>" required 
                        class="w-full bg-[#0f172a] border border-slate-700 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-indigo-500 transition placeholder-slate-600">
                </div>

                <div>
                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Kullanıcı Adı</label>
                    <input type="text" name="username" value="<%= user.username %>" required 
                        class="w-full bg-[#0f172a] border border-slate-700 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-indigo-500 transition placeholder-slate-600">
                </div>

                <div>
                    <label class="block text-xs font-semibold text-amber-400 uppercase tracking-wider mb-1">
                        <i class="fa-solid fa-triangle-exclamation mr-1"></i> E-Posta (Kurtarma İçin)
                    </label>
                    <input type="email" name="email" value="<%= user.email || '' %>" placeholder="ornek@gmail.com" required 
                        class="w-full bg-[#0f172a] border border-amber-500/30 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-amber-500 transition placeholder-slate-600">
                </div>

                <div>
                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Biyografi</label>
                    <textarea name="biyografi" rows="2" placeholder="Kendinden bahset..."
                        class="w-full bg-[#0f172a] border border-slate-700 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-indigo-500 transition placeholder-slate-600 resize-none"><%= user.biyografi || '' %></textarea>
                </div>

                <div class="pt-4 border-t border-slate-700/50">
                    <a href="/change-password" class="group flex items-center justify-between w-full bg-slate-800 hover:bg-slate-700 border border-slate-600 p-4 rounded-xl transition cursor-pointer no-underline">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 rounded-full bg-indigo-500/20 flex items-center justify-center text-indigo-400">
                                <i class="fa-solid fa-lock"></i>
                            </div>
                            <div class="text-left">
                                <div class="text-white font-semibold text-sm">Şifre Değiştir</div>
                                <div class="text-xs text-slate-400">Güvenliğin için şifreni yenile</div>
                            </div>
                        </div>
                        <i class="fa-solid fa-chevron-right text-slate-500 group-hover:text-white transition"></i>
                    </a>
                </div>

                <button type="submit" class="w-full bg-gradient-to-r from-indigo-600 to-violet-600 hover:from-indigo-700 hover:to-violet-700 text-white font-bold py-4 rounded-xl shadow-lg shadow-indigo-500/30 transition transform active:scale-95 flex items-center justify-center gap-2">
                    <i class="fa-solid fa-check"></i> DEĞİŞİKLİKLERİ KAYDET
                </button>

            </form>
        </div>
    </div>

</body>
</html>