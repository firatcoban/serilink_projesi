<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hesap Ayarları</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-[#0f172a] text-white min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-md bg-[#1e293b] rounded-3xl shadow-2xl border border-slate-700/50 overflow-hidden">
        
        <div class="bg-[#334155]/30 px-6 py-5 border-b border-slate-700/50 flex items-center justify-between backdrop-blur-sm">
            <h2 class="text-lg font-bold text-white flex items-center gap-2">
                <i class="fa-solid fa-user-gear text-indigo-400"></i> Hesap Ayarları
            </h2>
            <a href="/admin" class="text-xs font-medium text-slate-400 hover:text-white transition bg-slate-700/50 px-3 py-1.5 rounded-full">
                <i class="fa-solid fa-arrow-left mr-1"></i> Geri
            </a>
        </div>

        <div class="p-8">
            <form action="/settings/update" method="POST" enctype="multipart/form-data" class="space-y-6">
                
                <div class="flex flex-col items-center justify-center mb-6">
                    <div class="relative group">
                        <div class="w-28 h-28 rounded-full bg-slate-800 border-4 border-slate-700 overflow-hidden shadow-2xl group-hover:border-indigo-500 transition duration-300">
                            <% if(user.resim_url) { %>
                                <img src="<%= user.resim_url %>" class="w-full h-full object-cover">
                            <% } else { %>
                                <div class="w-full h-full flex items-center justify-center text-slate-600">
                                    <i class="fa-solid fa-user text-4xl"></i>
                                </div>
                            <% } %>
                        </div>
                        <label class="absolute bottom-1 right-1 bg-indigo-600 hover:bg-indigo-500 text-white p-2.5 rounded-full cursor-pointer shadow-lg transition-transform hover:scale-110 border-4 border-[#1e293b]">
                            <i class="fa-solid fa-camera text-sm"></i>
                            <input type="file" name="profil_resmi" class="hidden" onchange="this.form.submit()">
                        </label>
                    </div>
                    <p class="mt-3 text-xs font-medium text-slate-500">Profil resmini değiştirmek için ikona tıkla</p>
                </div>

                <div class="space-y-4">
                    
                    <div class="relative group">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                            <i class="fa-solid fa-signature text-slate-500 group-focus-within:text-indigo-400 transition"></i>
                        </div>
                        <input type="text" name="ad_soyad" value="<%= user.ad_soyad %>" required placeholder="Görünen İsim"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-11 pr-4 text-sm text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition shadow-sm">
                    </div>

                    <div class="relative group">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                            <i class="fa-solid fa-at text-slate-500 group-focus-within:text-indigo-400 transition"></i>
                        </div>
                        <input type="text" name="username" value="<%= user.username %>" required placeholder="Kullanıcı Adı"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-11 pr-4 text-sm text-white placeholder-slate-500 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition shadow-sm">
                    </div>

                    <div class="relative group">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                            <i class="fa-solid fa-envelope text-slate-500 group-focus-within:text-amber-400 transition"></i>
                        </div>
                        <input type="email" name="email" value="<%= user.email || '' %>" required placeholder="E-Posta (Kurtarma İçin)"
                            class="w-full bg-[#0f172a] border border-slate-700 rounded-xl py-3.5 pl-11 pr-4 text-sm text-white placeholder-slate-500 focus:outline-none focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition shadow-sm">
                    </div>
                </div>

                <div class="border-t border-slate-700/50 my-2"></div>

                <div class="space-y-3">
                    <a href="/change-password" class="flex items-center justify-between w-full bg-[#334155]/40 hover:bg-[#334155] border border-slate-600/50 hover:border-slate-500 text-slate-300 py-3.5 px-5 rounded-xl transition group">
                        <span class="text-sm font-medium flex items-center gap-2">
                            <i class="fa-solid fa-lock text-slate-500 group-hover:text-white transition"></i> Şifre Değiştir
                        </span>
                        <i class="fa-solid fa-chevron-right text-xs text-slate-600 group-hover:text-white transition"></i>
                    </a>

                    <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-4 rounded-xl shadow-lg shadow-indigo-600/20 transition transform active:scale-[0.98] flex items-center justify-center gap-2">
                        <i class="fa-solid fa-floppy-disk"></i> DEĞİŞİKLİKLERİ KAYDET
                    </button>
                </div>

            </form>
        </div>
    </div>
</body>
</html>